# 诗韵长安・峡谷寻踪 —— 技术答辩准备手册

> 本文档帮助你理解项目中每个技术点的**含义、作用、为什么用它**，便于评委提问时从容回答。

---

## 一、基础概念解释

### 1. HTML5 / CSS3 / JavaScript ES6+

| 名词 | 是什么 | 在项目中的作用 |
|------|--------|----------------|
| **HTML5** | 网页的骨架/结构语言，最新版本 | 定义页面所有内容（标题、图片、按钮等） |
| **CSS3** | 网页的样式/外观语言 | 控制颜色、布局、动画、响应式适配 |
| **JavaScript ES6+** | 网页的行为/交互语言 | 实现点击切换、AI对话、粒子动画等所有交互 |
| **纯原生** | 不使用React/Vue等框架 | 零依赖，加载更快，代码量可控 |

**评委可能问**：为什么不用框架？
**回答**：项目是单页展示型网站，用原生技术可以做到零构建、零依赖，用户打开即用，不需要npm安装、不需要编译打包，部署简单，加载极快。

---

### 2. Serverless（无服务器架构）

**是什么**：不需要自己买服务器、装系统、运维。代码上传到云平台（Vercel），平台自动运行，按调用次数收费。

**在项目中的作用**：我们的AI对话功能需要调用腾讯元器API，但API密钥不能暴露在前端代码里（别人能看到），所以放在Vercel的Serverless Function里做中转代理。

**通俗比喻**：就像外卖平台，你不需要自己开餐厅（买服务器），只需要把菜谱（代码）上传，平台帮你做好送出去。

**评委可能问**：Serverless和传统服务器有什么区别？
**回答**：传统服务器需要24小时运行、自己维护、按月付费；Serverless按调用次数付费，不用时不花钱，自动扩容，我们不需要管运维。

---

### 3. Vercel

**是什么**：一个云部署平台，专门用于部署前端项目和Serverless函数。

**在项目中的作用**：
- 托管我们的AI对话后端代理（Serverless Function）
- 代码推送到GitHub后自动部署，无需手动操作

**评委可能问**：为什么选Vercel？
**回答**：免费额度够用、部署简单（git push即自动上线）、全球CDN加速、原生支持Serverless Function。

---

### 4. GitHub Pages

**是什么**：GitHub提供的免费静态网页托管服务。

**在项目中的作用**：托管我们的主网页（HTML/CSS/JS/图片/视频），用户访问的地址就是 `junjieyang651-blip.github.io/fuchuang`。

**为什么用它**：免费、稳定、国内大部分地区可访问、代码推送后自动更新。

---

### 5. CDN（内容分发网络）

**是什么**：把文件分散存储到全球各地的服务器上，用户访问时从离自己最近的节点下载，速度更快。

**在项目中的作用**：TensorFlow.js 和 COCO-SSD 模型文件通过 CDN 加载，用户无论在哪都能快速下载这些AI模型。

---

## 二、AI技术部分

### 6. TensorFlow.js

**是什么**：Google开源的机器学习框架TensorFlow的JavaScript版本，能让AI模型直接在浏览器里运行。

**在项目中的作用**：加载目标检测模型，让用户上传插画后，**在浏览器里**直接识别出图片中有什么物体（人物、剑、酒杯等）。

**核心优势**：不需要服务器参与，所有AI计算都在用户自己的设备上完成，零延迟、零成本。

**评委可能问**：浏览器能跑AI模型不会很慢吗？
**回答**：我们用的是轻量级模型（MobileNet V2），专门为移动设备和浏览器优化过，推理一张图片只需要几百毫秒。

---

### 7. COCO-SSD 模型

**是什么**：一个预训练的目标检测模型。
- **COCO**：一个超大型图像数据集的名字（包含80类常见物体）
- **SSD**：Single Shot MultiBox Detector，一种检测算法，"看一眼就能识别出所有物体的位置和类别"

**在项目中的作用**：识别插画中的人物、道具、场景元素，输出物体名称+位置+置信度。

**我们的创新**：把英文标签翻译成古风中文（person→人物、sword→长剑、cup→酒杯），贴合盛唐主题。

**评委可能问**：COCO-SSD能识别哪些东西？
**回答**：可以识别80类日常物体，包括人、动物、交通工具、生活用品等。在我们的插画中主要识别人物、武器、器皿、建筑等元素。

---

### 8. MobileNet V2

**是什么**：Google开发的轻量级神经网络架构，专为手机和嵌入式设备设计。

**为什么用它**：它是COCO-SSD的"骨干网络"（backbone），负责提取图像特征。比大型网络（如ResNet）小很多，但准确率够用。

**通俗比喻**：如果大型模型是一辆卡车（装载多但慢），MobileNet就是一辆电动车（轻便快速，日常够用）。

---

### 9. YOLOv8

**是什么**：You Only Look Once（你只需要看一次），一种实时目标检测算法，v8是最新版本。

**在项目中的作用**：在本地Python环境中对9幅插画和视频逐帧做更精准的检测，生成详细的检测报告。

**与TensorFlow.js的区别**：
| | TensorFlow.js (浏览器端) | YOLOv8 (Python离线) |
|---|---|---|
| 运行位置 | 用户浏览器 | 我们的电脑 |
| 速度 | 实时但精度稍低 | 精度更高但需离线处理 |
| 用途 | 用户交互体验 | 生成检测报告/数据分析 |

**评委可能问**：为什么要用两套检测系统？
**回答**：浏览器端给用户提供实时交互体验，离线端做更精准的分析和报告生成，两者互补。

---

### 10. 腾讯元器（智能体平台）

**是什么**：腾讯的AI智能体开发平台，可以创建定制化的对话AI，类似于定制版ChatGPT。

**在项目中的作用**：我们在元器上创建了一个"盛唐文化导览助手"智能体，它了解我们项目的所有信息，用户可以在网页上直接和它对话，问插画故事、技术细节、文旅路线等。

**技术实现**：
1. 在腾讯元器平台创建智能体，配置知识库
2. 获取API密钥（AppKey + AppID）
3. 前端用户发消息 → 发送到我们的Vercel代理 → 代理转发给腾讯元器API → 返回AI回复

**评委可能问**：为什么不直接从前端调用元器API？
**回答**：API密钥不能暴露在前端代码里（任何人都能看到网页源代码），所以需要通过后端代理转发，密钥存在服务器环境变量中。

---

### 11. 腾讯元宝

**是什么**：腾讯的多模态AI创作工具，支持文本生成、图像生成等。

**在项目中的作用**：生成9幅国风AI插画。我们设计好Prompt（提示词），输入给元宝，它生成对应的插画。

**评委可能问**：Prompt是怎么设计的？
**回答**：我们的Prompt包含：英雄角色描述 + 西安地标场景 + 盛唐美学风格 + 画面构图要求。比如"上官婉儿执笔立于大雁塔下，唐风国画风格，金色暮光"。

---

### 12. CodeBuddy

**是什么**：腾讯的AI编程助手，类似GitHub Copilot，能辅助写代码。

**在项目中的作用**：辅助完成3800+行前端代码的编写，包括页面结构、CSS样式、交互逻辑等。

**评委可能问**：代码是AI写的还是你们写的？
**回答**：CodeBuddy是辅助工具，核心架构设计、功能规划、调试优化都是我们完成的。就像用计算器不代表你不会数学。

---

### 13. 腾讯开悟

**是什么**：腾讯的AI评测平台，用于评估模型和系统的效果。

**在项目中的作用**：对智能体的回答质量、对话流畅度进行评测，发现问题后迭代优化。

---

## 三、前端技术部分

### 14. Canvas API（画布）

**是什么**：HTML5提供的绘图接口，可以在网页上画任何图形、动画。

**在项目中的作用**：
- **粒子特效**：金色光点飘落动画，营造盛唐氛围
- **目标检测可视化**：在图片上画出检测到的物体边界框
- **技术架构图**：用Canvas绘制项目架构图

**评委可能问**：粒子动画怎么实现的？
**回答**：用 `requestAnimationFrame` 做60fps循环，每帧清除画布、更新每个粒子的位置（加入正弦摆动模拟飘落），重新绘制。根据页面面积动态控制粒子数量，保证性能。

---

### 15. CSS Grid / Flexbox

**是什么**：CSS的两种布局方式。
- **Flexbox**：一维布局（一行或一列内的排列）
- **Grid**：二维布局（行+列的网格系统）

**在项目中的作用**：实现响应式布局，同一套代码在手机、平板、电脑上都能正常显示。

**评委可能问**：怎么做到手机和电脑都能看？
**回答**：用CSS媒体查询（`@media`）检测屏幕宽度，配合Grid/Flexbox自动调整列数和间距。比如电脑上3列展示，手机上自动变成1列。

---

### 16. CSS变量

**是什么**：在CSS中定义的可复用的值，比如 `--accent-gold: #D4AF37`。

**在项目中的作用**：统一管理盛唐主题色（金色、红色、蓝色等），修改一处全局生效，保证视觉一致性。

---

### 17. Intersection Observer（交叉观察器）

**是什么**：浏览器提供的API，能检测元素是否进入了用户的可视区域。

**在项目中的作用**：
- **懒加载**：图片只有滚动到可见时才开始加载，节省流量
- **fade-in动画**：元素进入视野时触发淡入动画效果

**评委可能问**：为什么不直接加载所有图片？
**回答**：9幅高清插画+视频，全部预加载会导致首屏加载很慢。用懒加载可以让用户先看到内容，其他图片在后台按需加载。

---

### 18. Web Speech API（语音合成）

**是什么**：浏览器自带的语音能力，可以把文字转成语音朗读出来。

**在项目中的作用**：点击朗读按钮，用中文语音朗读插画的创作理念和设计描述。

**优势**：不需要任何外部服务或API，浏览器原生支持，零成本。

---

### 19. localStorage / sessionStorage

**是什么**：浏览器提供的本地存储，可以在用户设备上保存少量数据。
- **localStorage**：关闭浏览器后数据仍在
- **sessionStorage**：关闭标签页后数据消失

**在项目中的作用**：保存用户的一些设置和状态。

---

## 四、部署与架构

### 20. 前后端分离

**是什么**：前端（用户看到的网页）和后端（处理数据的服务器）分开部署。

**我们的架构**：
- 前端：GitHub Pages（展示网页）
- 后端：Vercel（AI对话代理）

**好处**：前端加载快、后端独立扩展、互不影响。

---

### 21. API 代理

**是什么**：前端不直接调用第三方API，而是先发给自己的后端，后端再转发给第三方。

**为什么需要**：
1. 保护API密钥不暴露
2. 绕过浏览器的跨域限制（CORS）
3. 可以在中间层做限流、缓存等控制

**流程**：用户浏览器 → 我们的Vercel后端 → 腾讯元器API → 原路返回

---

### 22. CORS（跨域资源共享）

**是什么**：浏览器的安全策略，默认不允许网页访问其他域名的API。

**举例**：我们的网页在 `github.io`，要调用 `yuanqi.tencent.com` 的API，浏览器会拦截。

**解决方案**：通过后端代理（同源策略不限制服务器间通信），或者API服务器设置允许跨域的响应头。

---

## 五、项目特色回答模板

### Q：项目的技术创新点在哪？

A：主要有三个层面：
1. **浏览器端AI推理** — 用TensorFlow.js让AI模型直接在用户浏览器中运行，无需服务器，用户隐私数据不用上传
2. **腾讯AI工具链闭环** — 元宝生成图片、元器做智能体、CodeBuddy写代码、开悟做评测，四大工具覆盖从创意到上线的全流程
3. **零框架轻量架构** — 纯原生代码+Serverless，无需安装部署，网址打开即用

### Q：这个项目有什么实际应用价值？

A：
1. **文旅宣传**：用年轻人熟悉的电竞IP来推广西安文旅，比传统宣传更有传播力
2. **AI导览**：游客到景区可以扫码和AI助手对话，了解景点历史
3. **创作工具**：Prompt生成器让更多人能用AI创作国风内容，形成UGC生态

### Q：如何保证AI对话的安全性？

A：
1. API密钥存储在Vercel环境变量中，不出现在任何前端代码里
2. 通过Serverless代理转发请求，用户无法直接获取密钥
3. 前端只发送用户消息，不接触任何认证信息

### Q：浏览器端跑AI模型，性能够用吗？

A：
1. 我们选的是MobileNet V2轻量模型，专为移动端设计
2. 模型大小只有几MB，首次加载后会被浏览器缓存
3. 实测推理一张图片200-500毫秒，用户体验流畅

### Q：为什么不用React/Vue这种流行框架？

A：
1. 项目是内容展示型，不是复杂交互应用，原生足够
2. 零构建依赖，git push即部署，不需要npm/webpack
3. 单文件便于理解和维护，所有代码一目了然
4. 加载更快，没有框架运行时的额外开销

### Q：你们团队的分工是怎样的？

（根据实际情况回答，建议提前准备）

### Q：遇到了哪些技术难题，怎么解决的？

A：
1. **AI对话跨域问题**：浏览器不允许直接调用腾讯API → 用Vercel Serverless做代理
2. **粒子动画在Tab切换后消失**：隐藏页面的canvas尺寸为0 → 切换页面后触发resize重新计算
3. **大文件视频加载慢**：62MB视频导致首屏慢 → 用 `preload="metadata"` 只预加载元数据，用户点击后才加载视频内容

---

## 六、数据与量化指标（加分项）

- 前端代码量：~3800行（单文件）
- AI插画数量：9幅（3系列×3幅）
- 支持检测物体类别：80类
- 粒子系统覆盖：全部8个页面模块
- AI对话上下文：最近10轮
- 模型加载时间：首次约3-5秒，后续缓存秒开
- 响应式断点：手机（<768px）、平板（768-1024px）、桌面（>1024px）
- 部署架构：GitHub Pages + Vercel Serverless，零运维成本